#include <iostream> 
#include <iomanip>
#include <cmath>
using namespace std;

int main()
{

long long int N, R;
if (N < 1000000)
    if (N>0)
    {
R = 1+(((N-1)*N)/2) + (((N)*(N - 1)*(N - 2 )*(N - 3))/24)
cout << R << endl;
    }
    return 0;
    }